export const people = ["Maya", "Gemma", "Sam"];
export const total = 1000;


const add = (a, b) => {
  return a + b;
}

const multiply = (a, b) => {
  return a * b;
}

export default multiply;

// default represents a fall back or main function for a modules
// can't use default and const / let declaration on the same line
